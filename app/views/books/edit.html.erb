<h1>Editing Book</h1>
  <% if @book.errors.any? %>
    <%= @book.errors.count %> errors prohibited this book from being saved
    <Ul>
      <% @book.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </Ul>
  <% end %>

<%= form_with model: @book1, url: book_path(@book1.id), method: :patch do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_field :body %>

  <%= f.submit 'Update Book' %>

<% end %>



<%= link_to 'Show', book_path(@book1.id) %>
<%= link_to "Back", "/books" %>